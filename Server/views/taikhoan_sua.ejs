<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>
        <%= title %>
    </title>
</head>

<body>
    <div class="container">
        <%- include('navbar'); %>
            <div class="card mt-3">
                <h5 class="card-header">
                    <%= title %>
                </h5>
                <div class="card-body">

                    <form action="/taikhoan/sua/<%= id %>" method="post">
                        <table>
                            <tr>
                                <td>Tên đăng nhập:</td>
                                <td><input type="text" name="tendangnhap" value="<%= tendangnhap %>" required></td>
                            </tr>

                            <tr>
                                <td>Mật khẩu:</td>
                                <td><input type="text" name="matkhau" value="<%= matkhau %>" required></td>
                            </tr>
                            <tr>
                                <td>Quyền:</td>
                                <td>
                                    <select name="quyen" required>
                            <option value="1" <%= quyen == '1' ? 'selected':'' %> >Admin</option>
                            <option value="2" <%= quyen == '2' ? 'selected':'' %> >Bác Sĩ</option>
                            <option value="3" <%= quyen == '3' ? 'selected':'' %> >Y tá</option>
                        </select>
                                </td>
                            </tr>

                            <tr>
                                <td></td>
                            </tr>
                        </table>
                        <button type="submit" class="btn btn-primary">Cập nhập</button> hoặc <a href="/taikhoan">Quay về danh sách</a>
                    </form>
                </div>
                <%- include('footer'); %>
            </div>
    </div>
    <%- include('javascript'); %>
</body>

</html>